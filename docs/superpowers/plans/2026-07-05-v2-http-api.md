# v2 子项目③ HTTP API 层实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用 FastAPI 分层应用包一层 HTTP API,复用现有 compiler/recorder/runner/storage/reporter,为子项目④ Web UI 提供 9 类端点。

**Architecture:** 分层:app.py(工厂+CORS+异常映射)、routes.py(薄路由)、service.py(ApiService 业务编排)、models.py(API 请求/响应模型);storage 加 list_summaries;CLI 加 serve 命令。run/dry-run 同步阻塞,本地无鉴权绑 127.0.0.1。现有核心逻辑零改动。

**Tech Stack:** Python 3.11、FastAPI、uvicorn、Pydantic v2、pytest、FastAPI TestClient(httpx)、ruff、mypy strict。

## Global Constraints

- Python `>=3.11`。
- 依赖新增:`fastapi>=0.110`、`uvicorn>=0.29`(httpx 已由 openai 引入)。
- 9 类端点:GET /api/cases、POST /api/cases/compile-nl、POST /api/cases/compile-recording、GET /api/cases/{id}、PUT /api/cases/{id}、POST /api/cases/{id}/validate、POST /api/cases/{id}/dry-run、POST /api/cases/{id}/run、GET /api/runs、GET /api/runs/{id}。
- run/dry-run 同步阻塞。实时录制不进 API;compile-recording 只接离线事件 JSON。
- 本地无鉴权;serve 默认绑 `127.0.0.1`;CORS 默认 `["http://localhost:5173", "http://127.0.0.1:5173"]`;绝不默认绑 0.0.0.0。
- CaseSummary / RunSummary 定义在 storage 层(api 复用),避免 storage 反向依赖 api。
- 单个计划/运行结果直接用 ExecutablePlan / RunResult 作 response_model。
- service 层无 HTTP 概念,可脱离 FastAPI 单测;service 的 get/load 显式处理"不存在→404 语义"。
- 异常映射:PlanValidationError→422、CompilationError→400、RecordingError→400、not-found→404、其他 StorageError/UiCaseCompilerError→500。响应体 `{"detail": ...}`。
- run 里某步 failed 返回 200 + status=failed 的 RunResult(不是 HTTP 错误)。
- 现有 schema/runner/reporter/compiler/recorder 核心逻辑与现有 CLI 命令零改动。
- 全程 ruff + mypy strict 绿。
- 提交信息结尾:`Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`。

## 现有契约(实现须对齐)

- `RuntimeConfig`(config.py):现有字段 browser/headless/timeout_ms/output_dir/screenshot_on_failure/trace_enabled/video_enabled/llm。`load_config()` 从环境变量读取。
- `LLMConfig`:api_key/base_url/model/timeout_s。
- `FileStore(root: Path)`:`.root` 属性、read_json/write_json。
- `CaseRepository(store)`:save_plan(plan)→Path、load_plan(id)→ExecutablePlan、mark_status(id, status)。plans 存于 `store.root/plans/{id}.json`。
- `RunRepository(store)`:save_result(result)→Path、load_result(id)→RunResult。runs 存于 `store.root/runs/{id}.json`。
- `ExecutablePlan`:id/name/source/base_url/parameters/environment/steps。
- `RunResult`:run_id/plan_id/status/started_at/ended_at/steps/trace_path/video_paths/report_path。
- `NaturalLanguageCompiler(provider).compile(text, context)`→ExecutablePlan(async)。
- `DeepSeekProvider(llm_config)`。
- `PageContext`:url/title/accessibility_tree/dom_summary/screenshot_path。
- `EventCollector().collect(raw)`→list[RecordedEvent]。
- `RecordingCompiler().compile(events, name)`→ExecutablePlan。
- `PlanRunner(config).run(plan, RunOptions)`→RunResult(async)。`RunOptions`:headless/output_dir/runtime_params/dry_run 等。
- `DryRunService(runner, case_repository).dry_run(plan, options)`→RunResult(async)。
- `validate_plan(data)`→ExecutablePlan(抛 PlanValidationError)。
- 异常家族(errors.py):UiCaseCompilerError 基类、PlanValidationError、CompilationError、RecordingError、StorageError。

---

## File Structure

- `pyproject.toml`(改) — 加 fastapi、uvicorn。
- `src/ui_case_compiler/config.py`(改) — 加 ApiConfig + RuntimeConfig.api。
- `src/ui_case_compiler/storage/case_repository.py`(改) — 加 CaseSummary + list_summaries。
- `src/ui_case_compiler/storage/run_repository.py`(改) — 加 RunSummary + list_summaries。
- `src/ui_case_compiler/api/__init__.py`(新)
- `src/ui_case_compiler/api/models.py`(新) — API 请求/响应模型。
- `src/ui_case_compiler/api/service.py`(新) — ApiService 编排。
- `src/ui_case_compiler/api/app.py`(新) — create_app 工厂 + CORS + 异常处理器。
- `src/ui_case_compiler/api/routes.py`(新) — 9 端点薄路由。
- `src/ui_case_compiler/cli/main.py`(改) — serve 命令。

任务顺序:1 依赖 → 2 ApiConfig → 3 storage list → 4 models → 5 ApiService → 6 app+routes → 7 CLI serve。

---

### Task 1: 添加 fastapi/uvicorn 依赖

**Files:**
- Modify: `pyproject.toml`(dependencies 列表)

**Interfaces:**
- Consumes: 无
- Produces: 可 `import fastapi`、`import uvicorn`。

- [ ] **Step 1: 修改 pyproject.toml**

把 `dependencies` 列表(现含 openai>=1.0.0 的那段)改为追加两项:

```toml
dependencies = [
  "playwright>=1.45.0",
  "pydantic>=2.7.0",
  "typer>=0.12.0",
  "jinja2>=3.1.0",
  "rich>=13.7.0",
  "openai>=1.0.0",
  "fastapi>=0.110.0",
  "uvicorn>=0.29.0"
]
```

- [ ] **Step 2: 安装依赖**

Run: `.venv/Scripts/python.exe -m pip install -e '.[dev]'`
Expected: 成功安装 fastapi、uvicorn、starlette 等,无错误。

- [ ] **Step 3: 验证可导入**

Run: `.venv/Scripts/python.exe -c "import fastapi, uvicorn; print(fastapi.__version__, uvicorn.__version__)"`
Expected: 打印两个版本号,无 ModuleNotFoundError。

- [ ] **Step 4: 提交**

```bash
git add pyproject.toml
git commit -m "build: 添加 fastapi/uvicorn 依赖用于 HTTP API

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: 新增 ApiConfig

**Files:**
- Modify: `src/ui_case_compiler/config.py`
- Test: `tests/test_config.py`

**Interfaces:**
- Consumes: 无
- Produces: `ApiConfig(host: str, port: int, cors_origins: list[str])`;`RuntimeConfig.api: ApiConfig`;`load_config()` 从 `API_HOST` / `API_PORT` 环境变量读取。

- [ ] **Step 1: 写失败测试**

在 `tests/test_config.py` 末尾追加:

```python
def test_api_config_defaults(monkeypatch) -> None:
    monkeypatch.delenv("API_HOST", raising=False)
    monkeypatch.delenv("API_PORT", raising=False)

    config = load_config()

    assert config.api.host == "127.0.0.1"
    assert config.api.port == 8000
    assert "http://localhost:5173" in config.api.cors_origins


def test_api_config_reads_environment(monkeypatch) -> None:
    monkeypatch.setenv("API_HOST", "0.0.0.0")
    monkeypatch.setenv("API_PORT", "9001")

    config = load_config()

    assert config.api.host == "0.0.0.0"
    assert config.api.port == 9001
```

- [ ] **Step 2: 运行测试确认失败**

Run: `.venv/Scripts/python.exe -m pytest tests/test_config.py -v --basetemp="$TEMP/uicase_pt" -p no:cacheprovider`
Expected: 两个新测试 FAIL(`'RuntimeConfig' object has no attribute 'api'`)。

- [ ] **Step 3: 实现 ApiConfig**

编辑 `src/ui_case_compiler/config.py`。在 `LLMConfig` 之后、`RuntimeConfig` 之前加:

```python
class ApiConfig(BaseModel):
    """Configuration for the local HTTP API server."""

    host: str = "127.0.0.1"
    port: int = Field(default=8000, gt=0)
    cors_origins: list[str] = Field(
        default_factory=lambda: [
            "http://localhost:5173",
            "http://127.0.0.1:5173",
        ]
    )
```

在 `RuntimeConfig` 加字段(在 `llm` 字段之后):

```python
    api: ApiConfig = Field(default_factory=ApiConfig)
```

在 `load_config()` 里,构造 RuntimeConfig 前加 api 读取,并把 api 传入返回:

```python
def load_config() -> RuntimeConfig:
    """Load runtime configuration, reading LLM and API settings from the environment."""

    llm = LLMConfig(
        api_key=os.environ.get("DEEPSEEK_API_KEY"),
        base_url=os.environ.get("LLM_BASE_URL", "https://api.deepseek.com"),
        model=os.environ.get("LLM_MODEL", "deepseek-chat"),
    )
    api = ApiConfig(
        host=os.environ.get("API_HOST", "127.0.0.1"),
        port=int(os.environ.get("API_PORT", "8000")),
    )
    return RuntimeConfig(llm=llm, api=api)
```

- [ ] **Step 4: 运行测试确认通过**

Run: `.venv/Scripts/python.exe -m pytest tests/test_config.py -v --basetemp="$TEMP/uicase_pt" -p no:cacheprovider`
Expected: 全部 PASS(含原有配置测试)。

- [ ] **Step 5: 类型与风格**

Run: `.venv/Scripts/python.exe -m ruff check src/ui_case_compiler/config.py tests/test_config.py && .venv/Scripts/python.exe -m mypy src`
Expected: ruff All checks passed;mypy Success。

- [ ] **Step 6: 提交**

```bash
git add src/ui_case_compiler/config.py tests/test_config.py
git commit -m "feat: 新增 ApiConfig(host/port/cors_origins,默认绑 127.0.0.1)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: storage 加 list_summaries

**Files:**
- Modify: `src/ui_case_compiler/storage/case_repository.py`
- Modify: `src/ui_case_compiler/storage/run_repository.py`
- Test: `tests/storage/test_file_store.py`

**Interfaces:**
- Consumes: `FileStore`、`ExecutablePlan`、`RunResult`(现有)。
- Produces:
  - `CaseSummary(BaseModel)`:id/name/source/step_count,定义在 case_repository.py。
  - `CaseRepository.list_summaries() -> list[CaseSummary]`(扫 `store.root/plans/*.json`)。
  - `RunSummary(BaseModel)`:run_id/plan_id/status/started_at,定义在 run_repository.py。
  - `RunRepository.list_summaries() -> list[RunSummary]`(扫 `store.root/runs/*.json`)。

- [ ] **Step 1: 写失败测试**

在 `tests/storage/test_file_store.py` 末尾追加(顶部若无则加 `from ui_case_compiler.storage.case_repository import CaseRepository, CaseSummary` 和 `from ui_case_compiler.storage.run_repository import RunRepository, RunSummary`,以及 `from ui_case_compiler.schema.steps import NavigateStep`、`from ui_case_compiler.schema.executable_plan import ExecutablePlan`):

```python
def test_case_repository_list_summaries(tmp_path) -> None:
    from ui_case_compiler.storage.file_store import FileStore

    repo = CaseRepository(FileStore(tmp_path))
    repo.save_plan(
        ExecutablePlan(
            id="p1",
            name="Plan One",
            source="manual",
            steps=[NavigateStep(id="step-001", url="https://example.test")],
        )
    )
    repo.save_plan(
        ExecutablePlan(
            id="p2",
            name="Plan Two",
            source="recording",
            steps=[NavigateStep(id="step-001", url="https://example.test")],
        )
    )

    summaries = repo.list_summaries()

    assert {s.id for s in summaries} == {"p1", "p2"}
    one = next(s for s in summaries if s.id == "p1")
    assert one.name == "Plan One"
    assert one.source == "manual"
    assert one.step_count == 1


def test_case_repository_list_summaries_empty(tmp_path) -> None:
    from ui_case_compiler.storage.file_store import FileStore

    repo = CaseRepository(FileStore(tmp_path))
    assert repo.list_summaries() == []


def test_run_repository_list_summaries(tmp_path) -> None:
    from datetime import UTC, datetime

    from ui_case_compiler.reporter.run_result import RunResult
    from ui_case_compiler.storage.file_store import FileStore

    repo = RunRepository(FileStore(tmp_path))
    repo.save_result(
        RunResult(
            run_id="r1",
            plan_id="p1",
            status="passed",
            started_at=datetime(2026, 7, 5, tzinfo=UTC),
            ended_at=datetime(2026, 7, 5, tzinfo=UTC),
            steps=[],
        )
    )

    summaries = repo.list_summaries()

    assert len(summaries) == 1
    assert summaries[0].run_id == "r1"
    assert summaries[0].plan_id == "p1"
    assert summaries[0].status == "passed"
```

- [ ] **Step 2: 运行测试确认失败**

Run: `.venv/Scripts/python.exe -m pytest tests/storage/test_file_store.py -v --basetemp="$TEMP/uicase_pt" -p no:cacheprovider`
Expected: 新测试 FAIL(`ImportError: cannot import name 'CaseSummary'` 或 `has no attribute 'list_summaries'`)。

- [ ] **Step 3: 实现 CaseRepository.list_summaries**

编辑 `src/ui_case_compiler/storage/case_repository.py`。顶部加 import:

```python
from pydantic import BaseModel
```

在 `CaseStatus` 定义之后加模型:

```python
class CaseSummary(BaseModel):
    id: str
    name: str
    source: str
    step_count: int
```

在 `CaseRepository` 类里加方法:

```python
    def list_summaries(self) -> list[CaseSummary]:
        plans_dir = self._store.root / "plans"
        if not plans_dir.exists():
            return []
        summaries: list[CaseSummary] = []
        for path in sorted(plans_dir.glob("*.json")):
            plan = ExecutablePlan.model_validate(self._store.read_json(path))
            summaries.append(
                CaseSummary(
                    id=plan.id,
                    name=plan.name,
                    source=plan.source,
                    step_count=len(plan.steps),
                )
            )
        return summaries
```

- [ ] **Step 4: 实现 RunRepository.list_summaries**

编辑 `src/ui_case_compiler/storage/run_repository.py`。顶部加:

```python
from datetime import datetime

from pydantic import BaseModel
```

在类定义前加模型:

```python
class RunSummary(BaseModel):
    run_id: str
    plan_id: str
    status: str
    started_at: datetime
```

在 `RunRepository` 类里加方法:

```python
    def list_summaries(self) -> list[RunSummary]:
        runs_dir = self._store.root / "runs"
        if not runs_dir.exists():
            return []
        summaries: list[RunSummary] = []
        for path in sorted(runs_dir.glob("*.json")):
            result = RunResult.model_validate(self._store.read_json(path))
            summaries.append(
                RunSummary(
                    run_id=result.run_id,
                    plan_id=result.plan_id,
                    status=result.status,
                    started_at=result.started_at,
                )
            )
        return summaries
```

- [ ] **Step 5: 运行测试确认通过**

Run: `.venv/Scripts/python.exe -m pytest tests/storage/test_file_store.py -v --basetemp="$TEMP/uicase_pt" -p no:cacheprovider`
Expected: 全部 PASS。

- [ ] **Step 6: 类型与风格**

Run: `.venv/Scripts/python.exe -m ruff check src/ui_case_compiler/storage/ tests/storage/test_file_store.py && .venv/Scripts/python.exe -m mypy src`
Expected: ruff All checks passed;mypy Success。

- [ ] **Step 7: 提交**

```bash
git add src/ui_case_compiler/storage/ tests/storage/test_file_store.py
git commit -m "feat: storage 加 list_summaries 与 CaseSummary/RunSummary

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: API 请求/响应模型

**Files:**
- Create: `src/ui_case_compiler/api/__init__.py`
- Create: `src/ui_case_compiler/api/models.py`
- Test: `tests/api/test_api_models.py`

**Interfaces:**
- Consumes: `PageContext`(现有)。
- Produces:
  - `CompileNlRequest`:text: str、context: PageContext、name: str | None = None。
  - `CompileRecordingRequest`:events: list[dict]、name: str = "Recorded Flow"。
  - `RunRequest`:params: dict[str, str] = {}、headed: bool = False。
  - `ValidateResponse`:valid: bool、plan_id: str、step_count: int。
  - `ErrorResponse`:detail: str。

- [ ] **Step 1: 写失败测试**

创建 `tests/api/test_api_models.py`:

```python
from ui_case_compiler.api.models import (
    CompileNlRequest,
    CompileRecordingRequest,
    RunRequest,
    ValidateResponse,
)
from ui_case_compiler.compiler.page_context_collector import PageContext


def test_compile_nl_request_parses() -> None:
    req = CompileNlRequest(
        text="Click Login",
        context=PageContext(url="https://example.test/login"),
    )
    assert req.text == "Click Login"
    assert req.context.url == "https://example.test/login"
    assert req.name is None


def test_compile_recording_request_defaults_name() -> None:
    req = CompileRecordingRequest(events=[{"type": "navigation", "timestamp": 0}])
    assert req.name == "Recorded Flow"


def test_run_request_defaults() -> None:
    req = RunRequest()
    assert req.params == {}
    assert req.headed is False


def test_validate_response_shape() -> None:
    resp = ValidateResponse(valid=True, plan_id="p1", step_count=3)
    assert resp.valid is True
    assert resp.step_count == 3
```

- [ ] **Step 2: 运行测试确认失败**

Run: `.venv/Scripts/python.exe -m pytest tests/api/test_api_models.py -v --basetemp="$TEMP/uicase_pt" -p no:cacheprovider`
Expected: FAIL(`ModuleNotFoundError: ... api.models`)。

- [ ] **Step 3: 创建 api 包与模型**

创建 `src/ui_case_compiler/api/__init__.py`(空文件)。

创建 `src/ui_case_compiler/api/models.py`:

```python
from __future__ import annotations

from pydantic import BaseModel, ConfigDict, Field

from ui_case_compiler.compiler.page_context_collector import PageContext


class CompileNlRequest(BaseModel):
    model_config = ConfigDict(extra="forbid")

    text: str
    context: PageContext
    name: str | None = None


class CompileRecordingRequest(BaseModel):
    model_config = ConfigDict(extra="forbid")

    events: list[dict] = Field(default_factory=list)
    name: str = "Recorded Flow"


class RunRequest(BaseModel):
    model_config = ConfigDict(extra="forbid")

    params: dict[str, str] = Field(default_factory=dict)
    headed: bool = False


class ValidateResponse(BaseModel):
    valid: bool
    plan_id: str
    step_count: int


class ErrorResponse(BaseModel):
    detail: str
```

- [ ] **Step 4: 运行测试确认通过**

Run: `.venv/Scripts/python.exe -m pytest tests/api/test_api_models.py -v --basetemp="$TEMP/uicase_pt" -p no:cacheprovider`
Expected: 全部 PASS。

- [ ] **Step 5: 类型与风格**

Run: `.venv/Scripts/python.exe -m ruff check src/ui_case_compiler/api/ tests/api/ && .venv/Scripts/python.exe -m mypy src`
Expected: ruff All checks passed;mypy Success。

- [ ] **Step 6: 提交**

```bash
git add src/ui_case_compiler/api/__init__.py src/ui_case_compiler/api/models.py tests/api/test_api_models.py
git commit -m "feat: API 请求/响应模型

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: ApiService 业务编排

**Files:**
- Create: `src/ui_case_compiler/api/service.py`
- Test: `tests/api/test_service.py`

**Interfaces:**
- Consumes: Task 2 `RuntimeConfig.api`;Task 3 `list_summaries`/CaseSummary/RunSummary;Task 4 models;现有 compiler/recorder/runner/storage。
- Produces:
  - `NotFoundError(UiCaseCompilerError)`——service 层显式 not-found 语义(app.py 映射 404)。定义在 service.py。
  - `ApiService(config: RuntimeConfig)`,方法:list_cases、compile_nl(async)、compile_recording、get_case、update_case、validate_case、dry_run(async)、run(async)、list_runs、get_run。

- [ ] **Step 1: 写失败测试**

创建 `tests/api/test_service.py`:

```python
from pathlib import Path

import pytest

from ui_case_compiler.api.models import CompileNlRequest, CompileRecordingRequest, RunRequest
from ui_case_compiler.api.service import ApiService, NotFoundError
from ui_case_compiler.compiler.page_context_collector import PageContext
from ui_case_compiler.config import RuntimeConfig, LLMConfig


def _config(tmp_path) -> RuntimeConfig:
    return RuntimeConfig(output_dir=tmp_path, llm=LLMConfig(api_key="sk-test"))


def _recording_events() -> list[dict]:
    return [
        {"type": "navigation", "timestamp": 0, "url": "https://example.test/login"},
        {
            "type": "click",
            "timestamp": 1,
            "element": {"tag": "button", "role": "button", "text": "Login"},
        },
    ]


def test_compile_recording_saves_and_lists(tmp_path) -> None:
    service = ApiService(_config(tmp_path))

    plan = service.compile_recording(
        CompileRecordingRequest(events=_recording_events(), name="Rec")
    )

    assert plan.source == "recording"
    summaries = service.list_cases()
    assert plan.id in {s.id for s in summaries}


def test_get_case_missing_raises_not_found(tmp_path) -> None:
    service = ApiService(_config(tmp_path))

    with pytest.raises(NotFoundError):
        service.get_case("does-not-exist")


def test_validate_case_reports_valid(tmp_path) -> None:
    service = ApiService(_config(tmp_path))
    plan = service.compile_recording(
        CompileRecordingRequest(events=_recording_events(), name="Rec")
    )

    resp = service.validate_case(plan.id)

    assert resp.valid is True
    assert resp.plan_id == plan.id
    assert resp.step_count == len(plan.steps)


def test_get_run_missing_raises_not_found(tmp_path) -> None:
    service = ApiService(_config(tmp_path))

    with pytest.raises(NotFoundError):
        service.get_run("no-run")


@pytest.mark.asyncio
async def test_compile_nl_uses_provider(tmp_path) -> None:
    service = ApiService(_config(tmp_path))
    fake_json = (
        '{"id": "nl", "name": "NL", "source": "natural_language",'
        ' "steps": [{"id": "step-001", "type": "navigate", "url": "https://example.test"}]}'
    )

    async def fake_generate(self, prompt: str) -> str:
        return fake_json

    from unittest.mock import patch

    with patch(
        "ui_case_compiler.compiler.deepseek_provider.DeepSeekProvider.generate_plan_json",
        fake_generate,
    ):
        plan = await service.compile_nl(
            CompileNlRequest(text="go", context=PageContext(url="https://example.test/login"))
        )

    assert plan.source == "natural_language"
    assert plan.id in {s.id for s in service.list_cases()}


@pytest.mark.asyncio
async def test_run_against_local_page(tmp_path) -> None:
    service = ApiService(_config(tmp_path))
    root = Path(__file__).resolve().parents[2]
    login_url = (root / "examples" / "pages" / "login.html").resolve().as_uri()
    login_plan = load_login_plan(root)
    service.update_case(login_plan.id, login_plan)

    result = await service.run(login_plan.id, RunRequest(params={"loginPageUrl": login_url}))

    assert result.status == "passed"
    assert result.run_id in {s.run_id for s in service.list_runs()}


def load_login_plan(root: Path):
    from ui_case_compiler.schema.validation import load_plan

    return load_plan(root / "examples" / "plans" / "login.json")
```

- [ ] **Step 2: 运行测试确认失败**

Run: `.venv/Scripts/python.exe -m pytest tests/api/test_service.py -v --basetemp="$TEMP/uicase_pt" -p no:cacheprovider`
Expected: FAIL(`ModuleNotFoundError: ... api.service`)。

- [ ] **Step 3: 实现 ApiService**

创建 `src/ui_case_compiler/api/service.py`:

```python
from __future__ import annotations

from ui_case_compiler.api.models import (
    CompileNlRequest,
    CompileRecordingRequest,
    RunRequest,
    ValidateResponse,
)
from ui_case_compiler.compiler.deepseek_provider import DeepSeekProvider
from ui_case_compiler.compiler.natural_language_compiler import NaturalLanguageCompiler
from ui_case_compiler.config import RuntimeConfig, load_config
from ui_case_compiler.errors import CompilationError, StorageError, UiCaseCompilerError
from ui_case_compiler.recorder.event_collector import EventCollector
from ui_case_compiler.recorder.recorder_session import RecordingCompiler
from ui_case_compiler.reporter.run_result import RunResult
from ui_case_compiler.runner.dry_run_service import DryRunService
from ui_case_compiler.runner.plan_runner import PlanRunner, RunOptions
from ui_case_compiler.schema.executable_plan import ExecutablePlan
from ui_case_compiler.schema.validation import validate_plan
from ui_case_compiler.storage.case_repository import CaseRepository, CaseSummary
from ui_case_compiler.storage.file_store import FileStore
from ui_case_compiler.storage.run_repository import RunRepository, RunSummary


class NotFoundError(UiCaseCompilerError):
    """Raised when a requested case or run does not exist."""


class ApiService:
    """Orchestrate the existing core for the HTTP API. No HTTP concepts here."""

    def __init__(self, config: RuntimeConfig | None = None) -> None:
        self._config = config or load_config()
        store = FileStore(self._config.output_dir)
        self._cases = CaseRepository(store)
        self._runs = RunRepository(store)

    def list_cases(self) -> list[CaseSummary]:
        return self._cases.list_summaries()

    async def compile_nl(self, req: CompileNlRequest) -> ExecutablePlan:
        if not self._config.llm.api_key:
            msg = "未配置模型 API key，请设置环境变量 DEEPSEEK_API_KEY"
            raise CompilationError(msg)
        provider = DeepSeekProvider(self._config.llm)
        compiler = NaturalLanguageCompiler(provider=provider)
        plan = await compiler.compile(req.text, req.context)
        self._cases.save_plan(plan)
        return plan

    def compile_recording(self, req: CompileRecordingRequest) -> ExecutablePlan:
        events = EventCollector().collect(req.events)
        plan = RecordingCompiler().compile(events, req.name)
        self._cases.save_plan(plan)
        return plan

    def get_case(self, case_id: str) -> ExecutablePlan:
        return self._load_case(case_id)

    def update_case(self, case_id: str, plan: ExecutablePlan) -> ExecutablePlan:
        if plan.id != case_id:
            from ui_case_compiler.errors import PlanValidationError

            msg = f"Plan id '{plan.id}' does not match path id '{case_id}'"
            raise PlanValidationError(msg)
        validated = validate_plan(plan.model_dump(mode="json"))
        self._cases.save_plan(validated)
        return validated

    def validate_case(self, case_id: str) -> ValidateResponse:
        plan = self._load_case(case_id)
        validated = validate_plan(plan.model_dump(mode="json"))
        return ValidateResponse(
            valid=True, plan_id=validated.id, step_count=len(validated.steps)
        )

    async def dry_run(self, case_id: str, req: RunRequest) -> RunResult:
        plan = self._load_case(case_id)
        service = DryRunService(
            runner=PlanRunner(self._config),
            case_repository=self._cases,
        )
        result = await service.dry_run(plan, self._run_options(req, dry_run=True))
        self._runs.save_result(result)
        return result

    async def run(self, case_id: str, req: RunRequest) -> RunResult:
        plan = self._load_case(case_id)
        result = await PlanRunner(self._config).run(plan, self._run_options(req))
        self._runs.save_result(result)
        return result

    def list_runs(self) -> list[RunSummary]:
        return self._runs.list_summaries()

    def get_run(self, run_id: str) -> RunResult:
        try:
            return self._runs.load_result(run_id)
        except StorageError as exc:
            raise NotFoundError(f"Run not found: {run_id}") from exc

    def _load_case(self, case_id: str) -> ExecutablePlan:
        try:
            return self._cases.load_plan(case_id)
        except StorageError as exc:
            raise NotFoundError(f"Case not found: {case_id}") from exc

    def _run_options(self, req: RunRequest, dry_run: bool = False) -> RunOptions:
        return RunOptions(
            headless=not req.headed,
            dry_run=dry_run,
            output_dir=self._config.output_dir,
            runtime_params=dict(req.params),
        )
```

- [ ] **Step 4: 运行测试确认通过**

Run: `.venv/Scripts/python.exe -m pytest tests/api/test_service.py -v --basetemp="$TEMP/uicase_pt" -p no:cacheprovider`
Expected: 全部 PASS(run 测试会真实启动 chromium 跑本地页)。

- [ ] **Step 5: 类型与风格**

Run: `.venv/Scripts/python.exe -m ruff check src/ui_case_compiler/api/service.py tests/api/test_service.py && .venv/Scripts/python.exe -m mypy src`
Expected: ruff All checks passed;mypy Success。

- [ ] **Step 6: 提交**

```bash
git add src/ui_case_compiler/api/service.py tests/api/test_service.py
git commit -m "feat: ApiService 业务编排层(复用现有核心,not-found 语义)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 6: FastAPI app + routes + 异常映射

**Files:**
- Create: `src/ui_case_compiler/api/app.py`
- Create: `src/ui_case_compiler/api/routes.py`
- Test: `tests/api/test_endpoints.py`

**Interfaces:**
- Consumes: Task 5 ApiService/NotFoundError;Task 4 models;现有异常家族。
- Produces:
  - `create_app(config: RuntimeConfig | None = None) -> FastAPI`。
  - 9 类端点(见 Global Constraints)。
  - 异常处理器映射到 HTTP 状态码。
  - `router: APIRouter`(routes.py),端点从 `request.app.state.service` 取 ApiService。

- [ ] **Step 1: 写失败测试**

创建 `tests/api/test_endpoints.py`:

```python
from pathlib import Path
from unittest.mock import patch

import pytest
from fastapi.testclient import TestClient

from ui_case_compiler.api.app import create_app
from ui_case_compiler.config import LLMConfig, RuntimeConfig


def _client(tmp_path, api_key: str | None = "sk-test") -> TestClient:
    config = RuntimeConfig(output_dir=tmp_path, llm=LLMConfig(api_key=api_key))
    return TestClient(create_app(config))


def _recording_body() -> dict:
    return {
        "events": [
            {"type": "navigation", "timestamp": 0, "url": "https://example.test/login"},
            {
                "type": "click",
                "timestamp": 1,
                "element": {"tag": "button", "role": "button", "text": "Login"},
            },
        ],
        "name": "Rec",
    }


def test_list_cases_empty(tmp_path) -> None:
    client = _client(tmp_path)
    resp = client.get("/api/cases")
    assert resp.status_code == 200
    assert resp.json() == []


def test_compile_recording_then_get_and_list(tmp_path) -> None:
    client = _client(tmp_path)

    resp = client.post("/api/cases/compile-recording", json=_recording_body())
    assert resp.status_code == 200
    plan_id = resp.json()["id"]

    listed = client.get("/api/cases")
    assert plan_id in {c["id"] for c in listed.json()}

    got = client.get(f"/api/cases/{plan_id}")
    assert got.status_code == 200
    assert got.json()["source"] == "recording"


def test_get_missing_case_returns_404(tmp_path) -> None:
    client = _client(tmp_path)
    resp = client.get("/api/cases/nope")
    assert resp.status_code == 404
    assert "detail" in resp.json()


def test_validate_endpoint(tmp_path) -> None:
    client = _client(tmp_path)
    plan_id = client.post("/api/cases/compile-recording", json=_recording_body()).json()["id"]

    resp = client.post(f"/api/cases/{plan_id}/validate")
    assert resp.status_code == 200
    assert resp.json()["valid"] is True


def test_compile_nl_missing_api_key_returns_400(tmp_path) -> None:
    client = _client(tmp_path, api_key=None)
    resp = client.post(
        "/api/cases/compile-nl",
        json={"text": "go", "context": {"url": "https://example.test/login"}},
    )
    assert resp.status_code == 400
    assert "API key" in resp.json()["detail"]


def test_compile_nl_with_patched_provider(tmp_path) -> None:
    client = _client(tmp_path)
    fake_json = (
        '{"id": "nl", "name": "NL", "source": "natural_language",'
        ' "steps": [{"id": "step-001", "type": "navigate", "url": "https://example.test"}]}'
    )

    async def fake_generate(self, prompt: str) -> str:
        return fake_json

    with patch(
        "ui_case_compiler.compiler.deepseek_provider.DeepSeekProvider.generate_plan_json",
        fake_generate,
    ):
        resp = client.post(
            "/api/cases/compile-nl",
            json={"text": "go", "context": {"url": "https://example.test/login"}},
        )
    assert resp.status_code == 200
    assert resp.json()["source"] == "natural_language"


def test_run_endpoint_against_local_page(tmp_path) -> None:
    client = _client(tmp_path)
    root = Path(__file__).resolve().parents[2]
    login_url = (root / "examples" / "pages" / "login.html").resolve().as_uri()
    plan_body = _login_plan_json(root)

    put = client.put(f"/api/cases/{plan_body['id']}", json=plan_body)
    assert put.status_code == 200

    resp = client.post(
        f"/api/cases/{plan_body['id']}/run",
        json={"params": {"loginPageUrl": login_url}},
    )
    assert resp.status_code == 200
    assert resp.json()["status"] == "passed"

    runs = client.get("/api/runs")
    assert resp.json()["run_id"] in {r["run_id"] for r in runs.json()}


def test_cors_header_present(tmp_path) -> None:
    client = _client(tmp_path)
    resp = client.get("/api/cases", headers={"Origin": "http://localhost:5173"})
    assert resp.headers.get("access-control-allow-origin") == "http://localhost:5173"


def _login_plan_json(root: Path) -> dict:
    import json

    return json.loads((root / "examples" / "plans" / "login.json").read_text(encoding="utf-8"))
```

- [ ] **Step 2: 运行测试确认失败**

Run: `.venv/Scripts/python.exe -m pytest tests/api/test_endpoints.py -v --basetemp="$TEMP/uicase_pt" -p no:cacheprovider`
Expected: FAIL(`ModuleNotFoundError: ... api.app`)。

- [ ] **Step 3: 实现 routes.py**

创建 `src/ui_case_compiler/api/routes.py`:

```python
from __future__ import annotations

from fastapi import APIRouter, Request

from ui_case_compiler.api.models import (
    CompileNlRequest,
    CompileRecordingRequest,
    RunRequest,
    ValidateResponse,
)
from ui_case_compiler.api.service import ApiService
from ui_case_compiler.reporter.run_result import RunResult
from ui_case_compiler.schema.executable_plan import ExecutablePlan
from ui_case_compiler.storage.case_repository import CaseSummary
from ui_case_compiler.storage.run_repository import RunSummary

router = APIRouter(prefix="/api")


def _service(request: Request) -> ApiService:
    service: ApiService = request.app.state.service
    return service


@router.get("/cases", response_model=list[CaseSummary])
def list_cases(request: Request) -> list[CaseSummary]:
    return _service(request).list_cases()


@router.post("/cases/compile-nl", response_model=ExecutablePlan)
async def compile_nl(request: Request, body: CompileNlRequest) -> ExecutablePlan:
    return await _service(request).compile_nl(body)


@router.post("/cases/compile-recording", response_model=ExecutablePlan)
def compile_recording(request: Request, body: CompileRecordingRequest) -> ExecutablePlan:
    return _service(request).compile_recording(body)


@router.get("/cases/{case_id}", response_model=ExecutablePlan)
def get_case(request: Request, case_id: str) -> ExecutablePlan:
    return _service(request).get_case(case_id)


@router.put("/cases/{case_id}", response_model=ExecutablePlan)
def update_case(request: Request, case_id: str, body: ExecutablePlan) -> ExecutablePlan:
    return _service(request).update_case(case_id, body)


@router.post("/cases/{case_id}/validate", response_model=ValidateResponse)
def validate_case(request: Request, case_id: str) -> ValidateResponse:
    return _service(request).validate_case(case_id)


@router.post("/cases/{case_id}/dry-run", response_model=RunResult)
async def dry_run(request: Request, case_id: str, body: RunRequest) -> RunResult:
    return await _service(request).dry_run(case_id, body)


@router.post("/cases/{case_id}/run", response_model=RunResult)
async def run_case(request: Request, case_id: str, body: RunRequest) -> RunResult:
    return await _service(request).run(case_id, body)


@router.get("/runs", response_model=list[RunSummary])
def list_runs(request: Request) -> list[RunSummary]:
    return _service(request).list_runs()


@router.get("/runs/{run_id}", response_model=RunResult)
def get_run(request: Request, run_id: str) -> RunResult:
    return _service(request).get_run(run_id)
```

说明:dry-run / run 的 body 有默认值需求——FastAPI 中带 body 的 POST,若前端不传 body 会 422。为允许空 body,body 参数给默认:把 `body: RunRequest` 改为 `body: RunRequest = RunRequest()` 不可变默认有风险,改用 `body: RunRequest | None = None` 并在函数内 `body = body or RunRequest()`。更新这两个端点:

```python
@router.post("/cases/{case_id}/dry-run", response_model=RunResult)
async def dry_run(request: Request, case_id: str, body: RunRequest | None = None) -> RunResult:
    return await _service(request).dry_run(case_id, body or RunRequest())


@router.post("/cases/{case_id}/run", response_model=RunResult)
async def run_case(request: Request, case_id: str, body: RunRequest | None = None) -> RunResult:
    return await _service(request).run(case_id, body or RunRequest())
```

- [ ] **Step 4: 实现 app.py**

创建 `src/ui_case_compiler/api/app.py`:

```python
from __future__ import annotations

from fastapi import FastAPI, Request
from fastapi.middleware.cors import CORSMiddleware
from fastapi.responses import JSONResponse

from ui_case_compiler.api.routes import router
from ui_case_compiler.api.service import ApiService, NotFoundError
from ui_case_compiler.config import RuntimeConfig, load_config
from ui_case_compiler.errors import (
    CompilationError,
    PlanValidationError,
    RecordingError,
    StorageError,
    UiCaseCompilerError,
)


def create_app(config: RuntimeConfig | None = None) -> FastAPI:
    config = config or load_config()
    app = FastAPI(title="UI Case Compiler API")
    app.add_middleware(
        CORSMiddleware,
        allow_origins=config.api.cors_origins,
        allow_methods=["*"],
        allow_headers=["*"],
    )
    app.state.service = ApiService(config)
    app.include_router(router)
    _register_exception_handlers(app)
    return app


def _register_exception_handlers(app: FastAPI) -> None:
    def _json(status: int, detail: str) -> JSONResponse:
        return JSONResponse(status_code=status, content={"detail": detail})

    @app.exception_handler(NotFoundError)
    async def _not_found(request: Request, exc: NotFoundError) -> JSONResponse:
        return _json(404, str(exc))

    @app.exception_handler(PlanValidationError)
    async def _validation(request: Request, exc: PlanValidationError) -> JSONResponse:
        return _json(422, str(exc))

    @app.exception_handler(CompilationError)
    async def _compilation(request: Request, exc: CompilationError) -> JSONResponse:
        return _json(400, str(exc))

    @app.exception_handler(RecordingError)
    async def _recording(request: Request, exc: RecordingError) -> JSONResponse:
        return _json(400, str(exc))

    @app.exception_handler(StorageError)
    async def _storage(request: Request, exc: StorageError) -> JSONResponse:
        return _json(500, str(exc))

    @app.exception_handler(UiCaseCompilerError)
    async def _base(request: Request, exc: UiCaseCompilerError) -> JSONResponse:
        return _json(500, str(exc))
```

注意异常处理器注册顺序:FastAPI 按异常类型精确匹配,子类处理器优先。NotFoundError 是 UiCaseCompilerError 子类,但 FastAPI 用最具体的注册类型匹配,已分别注册,OK。

- [ ] **Step 5: 运行测试确认通过**

Run: `.venv/Scripts/python.exe -m pytest tests/api/test_endpoints.py -v --basetemp="$TEMP/uicase_pt" -p no:cacheprovider`
Expected: 全部 PASS(run 端点真实跑 chromium)。

- [ ] **Step 6: 类型与风格**

Run: `.venv/Scripts/python.exe -m ruff check src/ui_case_compiler/api/ tests/api/ && .venv/Scripts/python.exe -m mypy src`
Expected: ruff All checks passed;mypy Success。

- [ ] **Step 7: 提交**

```bash
git add src/ui_case_compiler/api/app.py src/ui_case_compiler/api/routes.py tests/api/test_endpoints.py
git commit -m "feat: FastAPI app + 9 端点路由 + 异常映射

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 7: CLI serve 命令

**Files:**
- Modify: `src/ui_case_compiler/cli/main.py`
- Test: `tests/cli/test_cli.py`

**Interfaces:**
- Consumes: Task 6 `create_app`;`load_config`(现有);uvicorn。
- Produces: `serve` CLI 命令,启动 uvicorn,默认绑 config.api.host/port。

- [ ] **Step 1: 写失败测试**

在 `tests/cli/test_cli.py` 末尾追加:

```python
def test_serve_command_invokes_uvicorn() -> None:
    with patch("uvicorn.run") as mock_run:
        result = runner.invoke(app, ["serve", "--port", "9123"])

    assert result.exit_code == 0
    assert mock_run.called
    kwargs = mock_run.call_args.kwargs
    assert kwargs["port"] == 9123
    assert kwargs["host"] == "127.0.0.1"
```

- [ ] **Step 2: 运行测试确认失败**

Run: `.venv/Scripts/python.exe -m pytest tests/cli/test_cli.py::test_serve_command_invokes_uvicorn -v --basetemp="$TEMP/uicase_pt" -p no:cacheprovider`
Expected: FAIL(no such command 'serve',exit_code=2)。

- [ ] **Step 3: 实现 serve 命令**

编辑 `src/ui_case_compiler/cli/main.py`。顶部加 import:

```python
import uvicorn

from ui_case_compiler.api.app import create_app
```

新增命令选项常量(在其他 OPTION 常量附近):

```python
HOST_OPTION = typer.Option(None, "--host", help="API 绑定主机，默认 127.0.0.1。")
PORT_OPTION = typer.Option(None, "--port", help="API 端口，默认 8000。")
```

新增命令(放在 config 命令附近):

```python
@app.command("serve")
def serve_command(
    host: str | None = HOST_OPTION,
    port: int | None = PORT_OPTION,
) -> None:
    """启动本地 HTTP API 服务（默认绑 127.0.0.1，仅供本地使用）。"""

    config_value = load_config()
    uvicorn.run(
        create_app(config_value),
        host=host or config_value.api.host,
        port=port or config_value.api.port,
    )
```

- [ ] **Step 4: 运行测试确认通过**

Run: `.venv/Scripts/python.exe -m pytest tests/cli/test_cli.py::test_serve_command_invokes_uvicorn -v --basetemp="$TEMP/uicase_pt" -p no:cacheprovider`
Expected: PASS。

- [ ] **Step 5: 全量测试 + 类型 + 风格**

Run: `.venv/Scripts/python.exe -m pytest -q --basetemp="$TEMP/uicase_pt" -p no:cacheprovider && .venv/Scripts/python.exe -m ruff check . && .venv/Scripts/python.exe -m mypy src`
Expected: pytest 全绿(比子项目② 的 66 多出:config 2 + storage 3 + api models 4 + service 6 + endpoints 9 + cli serve 1 = +25，约 91);ruff All checks passed;mypy Success。

- [ ] **Step 6: 提交**

```bash
git add src/ui_case_compiler/cli/main.py tests/cli/test_cli.py
git commit -m "feat: 新增 serve 命令启动 HTTP API

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## 端到端手动验证(可选,需用户)

```powershell
$env:DEEPSEEK_API_KEY = "<your-key>"   # 若要测 compile-nl
ui-case serve
# 另开终端:
# curl http://127.0.0.1:8000/api/cases
# curl -X POST http://127.0.0.1:8000/api/cases/compile-recording -H "Content-Type: application/json" -d '{...}'
# 浏览器打开 http://127.0.0.1:8000/docs 看 FastAPI 自动文档
```

Expected: `/docs` 显示 9 类端点;GET /api/cases 返回 JSON 列表。

---

## Self-Review

**Spec coverage:**
- fastapi/uvicorn 依赖 → Task 1 ✓
- ApiConfig(host/port/cors,默认 127.0.0.1)→ Task 2 ✓
- storage list_summaries + CaseSummary/RunSummary 在 storage 层 → Task 3 ✓
- API 请求/响应模型 → Task 4 ✓
- ApiService 编排 + not-found 语义 + 缺 api_key 检查 → Task 5 ✓
- 9 类端点 + create_app + CORS + 异常映射 → Task 6 ✓
- serve 命令绑 127.0.0.1 → Task 7 ✓
- run/dry-run 同步阻塞(async await)→ Task 5/6 ✓
- 直接用 ExecutablePlan/RunResult 作 response_model → Task 6 ✓
- PUT id 不一致 → 422 → Task 5 update_case ✓
- 现有核心零改动 → 仅改 config/storage/cli,新增 api/ ✓

**Placeholder scan:** 无 TBD/TODO;每个 code step 含完整代码;命令含预期输出。✓

**Type consistency:**
- `ApiService(config)` 与各方法签名在 Task 5 定义,Task 6 routes 调用一致 ✓
- `create_app(config)` Task 6 定义,Task 7 调用一致 ✓
- `NotFoundError` Task 5 定义,Task 6 app.py import + 映射 404 一致 ✓
- CaseSummary/RunSummary(storage 层)Task 3 定义,Task 5 list 返回、Task 6 response_model 一致 ✓
- RunRequest/CompileNlRequest/CompileRecordingRequest/ValidateResponse Task 4 定义,Task 5/6 一致 ✓
- `_run_options` 用 RunOptions(headless/dry_run/output_dir/runtime_params)与现有 RunOptions 字段一致 ✓

**风险标注:**
- Task 6 dry-run/run 端点用 `body: RunRequest | None = None` + `body or RunRequest()` 允许空 body,已在 Step 3 说明。
- 异常处理器同时注册 NotFoundError 和其父类 UiCaseCompilerError;FastAPI 按最具体匹配,NotFoundError→404 优先于 UiCaseCompilerError→500。Task 6 Step 4 已注明。
